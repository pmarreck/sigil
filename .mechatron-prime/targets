# Mechatron Prime CI targets for sigil (Thelio x86_64-linux worker).
# One valid Nix flake attribute per line.
#
# checks.x86_64-linux.build is deliberately absent: it evaluates to the same
# derivation as packages.x86_64-linux.default, so listing both would just build
# the same store path twice in a single-queue worker.
packages.x86_64-linux.default
checks.x86_64-linux.test
checks.x86_64-linux.test-cli
checks.x86_64-linux.test-symbols
